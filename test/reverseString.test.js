import reverseString from "../src/reverseString";
test("checking the reverse fuction", () => {
  expect(reverseString("hello")).toBe("olleh");
});

test("checking the reverse function", () => {
  expect(reverseString("suraj")).toBe("jarus");
});
